def str_func(user_string):
    """Makes each word uppercased in a string"""
    new_string = user_string.upper()
    return new_string


def str_title(user_string):
    """Converts each word to a titled one in a string"""
    new_string = user_string.title()
    return new_string
